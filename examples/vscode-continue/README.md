# VS Code Continue + Telegraph MCP

## Setup

1. Open Continue's `config.json`.
2. Merge the [`config.json`](./config.json) block into your settings.
3. Replace `0xyour_key_here` with your EVM private key.
