# Cursor + Telegraph MCP

## Setup

1. Open Cursor Settings → MCP → Add new MCP server.
2. Paste the JSON from [`cursor_mcp_config.json`](./cursor_mcp_config.json).
3. Replace `0xyour_key_here` with your EVM private key.

## Config

See [`cursor_mcp_config.json`](./cursor_mcp_config.json).
